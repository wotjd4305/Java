*interfate FRUIT{ int APPLE =1, B=2,C=3;}
->interface에서 정의할 경우 final static int임

*스위치문은 어떤 타입을 쓸수있는가?
->원시타입, enum... 우리가 만든 객체타입불가(Fruit)
->enum을 통해 가독성 획득.

*enum - 열거형(연관된 상수들의 집합), 사실상 클래스임(생성자가능)
->생성자, 필드, 메소드도 가질 수 있음.
장점
1)인스턴드 생성과 상속 방지
2)코드 단순
3)구현의 의도를 열거형인것을 캐치
4)변경되지 않도록 보장.
5)클래스가 하는일은 얘도 함.
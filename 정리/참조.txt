*복제
->값에 의한 복제, 기본 데이터 타입이니까.
1)int a= 1;  int b = a; b= 2; System.out.println(a)// 1출력

*참조
1)A a = new A(1); A b = a; b.id = 2; a.id // 2출력
->인스턴스를 새로 만들지 않으니, a와 b는 같은 A인스턴스를 가르킴.
->바꾸면 같이 바뀐다는 뜻.
->A c = new A(3);으로 새로 만든 값은 같은 A인스턴스는 갖지않음.

*참조와 파라미터
->똑같은 맥락으로 당연하다 느껴지도록 인지.